@using Quixduell.Blazor.Shared.StudysetView.RatingComponent

<div class="rating-container row w-100">
    @for (int i = 1; i <= _maxRating; i++)
    {
        <RatingStarComponent Id="@i" IsFilled="@IsStarFilled(i)" OnToggle="@ToggleStar" />
    }
</div>